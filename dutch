#!/usr/bin/env bash
# Dutch SRS launcher. Run from anywhere, finds its own folder.
# Usage: ./dutch review     ./dutch stats     ./dutch seed
set -e
HERE="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec python3 "$HERE/app/cli.py" "$@"
